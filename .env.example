# Kopiér til .env. Visualisering kræver mindst PORT + ANTHROPIC_API_KEY.
PORT=3000
API_PORT=3000
ANTHROPIC_API_KEY=sk-ant-api03-...
# Valgfri — uden PostgreSQL gemmes ingen møde-historik; appen kører stadig.
# Med Docker: pnpm run db:setup
# DATABASE_URL=postgresql://speciale:speciale@127.0.0.1:5432/speciale
